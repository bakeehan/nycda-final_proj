<h1>Brackets#index</h1>
<p>Find me in app/views/brackets/index.html.erb</p>

<% @brackets.each do |b| %>
	<%= b.title %><br>
	<%if !b.avatar.file.nil? %>
		<img src="<%= b.avatar.url %>">
	<% end %>
<% end %>